query WorkoutWitId($workoutId: ID!) {
  workout(id: $workoutId) {
    id
    title
    backgroundImage
    sets
    reps
    exercises {
      id
      title
      description
      image
      type
    }
    createdAt
  }
}